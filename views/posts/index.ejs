<%- include('../partials/header') %>

    <h1>Posts Index</h1>

    <ul class="posts">
      <!-- posts variable comes from the object
      passed to the render function for this template in
      the posts.js router                 👇
      (i.e. res.render('posts/index', {posts: posts}); )
    -->
      <% for (let post of posts) { %>
        <a href="/posts/show/<%= post.id %>"><li><%= post.title %></li></a>
      <% } %>
    </ul>

<%- include('../partials/footer') %>
